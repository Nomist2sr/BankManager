cmake_minimum_required(VERSION 3.16)
SET(CMAKE_BUILD_TYPE)
project(BankManager)

# LINUX
if(UNIX)
  set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++20 -W -Wall -Wextra")
  endif(UNIX)

# CALL SUBDIRECTORY TO COMPILE
add_subdirectory(SRC)